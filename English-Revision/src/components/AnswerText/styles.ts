import styled from 'styled-components'

import Colors from '../../styles/colors.json'

export const TextAnswerStyles = styled.h2`
  font-weight: bold;
  font-family: 'Heebo';
  font-size: 1.4rem;

  color: ${Colors.white};

  margin-top: 24px;
`;